import { useEffect } from "react";
import { useParams } from "react-router";


export const Cards = () => {
  
  const params = useParams();
  
  useEffect(() => {

  }, []);

  return (
  <div className="Cards">
  </div>
  );
}